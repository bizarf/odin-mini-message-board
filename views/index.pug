extends layout

block content
  h1= title
  p Welcome to my #{title}

  a(class="button" href="./new") Add message

  each message in messages
    div 
      h4= message.user
      p= message.text
      p= message.added
    